import { EnrollmentSchedule } from './enrollment-schedule';

describe('EnrollmentSchedule', () => {
  it('should create an instance', () => {
    expect(new EnrollmentSchedule()).toBeTruthy();
  });
});
